<%= link_to "< Articles", root_path %>

<h1><%= @article.title %></h1>

<p style="font-family: monospace"><%= @article.body %></p>

<ul>
  <li><%= link_to "Edit", edit_article_path(@article) %></li>
  <li><%= link_to "Delete", @article, data: {
    turbo_method: :delete,
    turbo_confirm: "Are you sure?",
  } %></li>
</ul>
